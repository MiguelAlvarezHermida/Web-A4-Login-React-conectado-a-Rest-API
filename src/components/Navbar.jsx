import { AppBar, Toolbar, Typography } from "@mui/material";

function Navbar() {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography variant="h6">Web A2 Login con React y Material UI </Typography>
      </Toolbar>
    </AppBar>
  );
}

export default Navbar;
